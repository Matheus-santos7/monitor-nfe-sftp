Coloque aqui a chave privada SSH do bastion (`bastion.pem`) e aponte `NERUS_SSH_KEY_HOST_PATH` no `.env`.

```bash
chmod 600 secrets/bastion.pem
```

Este diretório não vai para o Git, exceto este README.
